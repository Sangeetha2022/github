export class BootstrapTable {

    async BootstrapTableHTMLGeneration(formData, screenData, details, callback) {
        console.log('enter into the bootstrap table generation');
        callback('enter into the bootstrap table generation');
    }
    
}