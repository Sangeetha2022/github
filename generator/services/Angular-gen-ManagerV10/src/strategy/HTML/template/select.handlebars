<div>
   <select id="{{components.attributes.id}}" class="{{components.cssClassName}}" {{#if
      components.attributes.entityDetails.entityName}}[(ngModel)]="{{components.attributes.entityDetails.entityName}}.{{components.attributes.entityDetails.fields.name}}"
      {{/if}}>
      {{{components.optionstag}}}
   </select>
</div>