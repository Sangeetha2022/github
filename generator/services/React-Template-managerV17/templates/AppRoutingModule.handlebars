{{#each importComponent}}
import {{classname}} from "./Components/{{classname}}/{{classname}}";
{{/each}}

var routes: any = [
  {{#each componentPath}}
    {
        path: "{{path}}",
        name: "{{component}}",
        component: {{component}},
        layout: "/auth",
    },
  {{/each}}
]

export default routes;

