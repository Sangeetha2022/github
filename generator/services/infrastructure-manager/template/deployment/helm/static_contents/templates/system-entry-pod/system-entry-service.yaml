apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.prefix }}-desktop
  namespace: {{ .Values.app.namespace }}
spec:
  type: NodePort
  ports:
  - name: nginx-app
    port: 80
    targetPort: 80
    NodePort: 31010
  selector:
    app: {{ .Values.app.prefix }}-desktop