apiVersion: v1
kind: Service
metadata:
  name: gep-dev-system-entry
  namespace: gep-dev-201902
spec:
  type: NodePort
  ports:
  - name: nginx-app
    port: 80
    targetPort: 80
  selector:
    app: gep-dev-system-entry
